#include <stdio.h>
#include <debugging.h>
#include <assert.h>

/*
struct TestDefinition 
{
  void (*function)(void);
  char* name;
};

inline void run_tests(struct TestDefinition* tests, int num_tests)
{

  for( int i = 0; i < num_tests ; i++)
  {
    printf("- Running %s ...\n",tests[i].name);
    // run the function
    tests[i].function();
  }
}
*/
